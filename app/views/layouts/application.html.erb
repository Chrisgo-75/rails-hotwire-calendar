<!DOCTYPE html>
<html>
  <head>
    <title>HotwireCal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <%= render("shared/navigation", title: "Hotwire Calendar") %>

    <main class="mx-auto max-w-full px-6 py-4 sm:py-6">
      <%= yield %>
    </main>

    <div aria-live="assertive" class="fixed inset-0 flex items-end px-2 py-3 pointer-events-none sm:p-3">
      <div class="w-full flex flex-col items-center space-y-4" id="flash_messages">
        <%= render("shared/flash") %>
      </div>
    </div>
  </body>
</html>
